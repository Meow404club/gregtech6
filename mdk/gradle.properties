# Mod 元数据（展开进 META-INF/mods.toml；GTCEu 同构：信息放 gradle.properties）
# P15 石匠骨架后本文件 = 控制器目录（mdk/）级共享属性：节点 projectDir =
# mdk/versions/<node>，Project.property() 向上可读父项目目录（2026-09-03 scratch 实证），
# 故双节点共享本文件，零重复。
# 版本参数已下沉节点目录：1.20.1 → versions/1.20.1-forge/gradle.properties（deps.minecraft/deps.forge）
#                        1.21.1 → versions/1.21.1-neoforge/gradle.properties（deps.minecraft/deps.neoforge）
mod_id = gt6
mod_name = GregTech 6
mod_version = 0.1.0
mod_license = LGPL-3.0-or-later
mod_description = GregTech 6, ported to MinecraftForge 1.20.1.
mod_authors = GregoriusT (original GregTech 6), brokestar233 (port contributor)
